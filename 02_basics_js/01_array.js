// array

const myArr = [0,1,2,3,4,5]

// console.log(myArr[1]);

// Array methods

// myArr.push(6) // adds element at the last
// myArr.pop() // removes the element from the last

// myArr.unshift(5) // it shifts the new element at the first index
// myArr.shift() // it removes the element from the first index

// console.log(myArr.includes(8)); // it checks whether the element is present or not, and it returns bool value
// console.log(myArr.indexOf(8)); // it returns the index of element if present else returns -1
// const newArr = myArr.join()

// console.log(myArr);
// console.log( typeof newArr);

// slice, splice

console.log('A', myArr);

// const myn1 = myArr.slice(2,5) // slice method doesn't manipulate the original array, it just copy the sliced elements excluding the last element of the parameter

// console.log(myn1)
// console.log('B', myArr);

// const myn2 = myArr.splice(2,5) // splice method manipulate the original array
// console.log('C', myArr);
// console.log(myn2);